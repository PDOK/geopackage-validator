[tool:pytest]
addopts = --flakes --cache-clear --black
filterwarnings =
    ignore:the imp module is deprecated in favour of importlib.*:DeprecationWarning

[zest.releaser]
# TODO: Releasing to pypi is done automatically by github (once set up)
release = no

